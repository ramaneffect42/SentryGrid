export const SYNC_SERVER_BASE_URL = 'http://192.168.1.10:4000';
export const SYNC_ENDPOINT = `${SYNC_SERVER_BASE_URL}/api/sync`;

// Replace the IP above with the LAN IP address of the machine running `node server.js`.
